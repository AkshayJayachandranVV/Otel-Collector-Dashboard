receivers:
  otlp:
    protocols:
      http:  # Enables OTLP/HTTP receiver on port 4318 by default
 
exporters:
  prometheus:
    endpoint: "0.0.0.0:8889"  # Prometheus will scrape from this endpoint
 
service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [prometheus]
 